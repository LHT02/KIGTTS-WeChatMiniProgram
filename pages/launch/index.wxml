<view class="launch-page page route-launch {{themeClass}} {{leaving ? 'leaving' : ''}}">
  <view class="launch-brand">
    <image class="launch-logo" src="{{logoSrc}}" mode="aspectFit" />
  </view>
</view>
