# KIGTTS 微信小程序修改记录

## 2026-07-01

- 便捷字幕快捷文本区末尾 `+` 卡片去掉描边。
- `+` 卡片行为改为添加当前已上屏的大字幕文本到当前快捷文本分组，不再跳转编辑页。
- 默认字幕字体大小改为 45% 对应的 `90rpx`，默认文本对齐改为左对齐，面向新用户和新初始化配置生效。
- 快捷文本内置预设调整为通用、扩列、拍照、后勤四组，面向新用户和新初始化配置生效。
